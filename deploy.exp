#!/usr/bin/expect -f
set timeout 300
spawn ssh -o StrictHostKeyChecking=no aiclex@135.235.218.93 "cd /home/aiclex/AIAGENT && git pull && npm install && npm run build && pm2 restart zonvo-app"
expect {
    "password:" {
        send "Umesh@2003##\r"
        exp_continue
    }
    eof
}
